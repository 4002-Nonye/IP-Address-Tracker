import React from "react";
import { MapContainer, TileLayer, useMap,Popup,Marker } from "react-leaflet";

const Map = () => {
  const position = [51.505, -0.09];
  return (
    <div className="object-contain w-9/12 relative -z-1">
   map
    </div>
  );
};

export default Map;
